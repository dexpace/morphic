openapi: 3.1.0
info: {title: LiteralConst, version: "1.0.0"}
paths: {}
components:
  schemas:
    Version:
      const: "v1"
    # A const the IR's Value model cannot represent degrades to the schemaless
    # top type rather than to a Literal whose value lies about what was written.
    Opaque:
      const: !!binary "###"
