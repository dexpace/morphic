openapi: 3.1.0
info: {title: ExtensionPromotion, version: "1.0.0"}
paths:
  /x:
    get:
      operationId: getX
      deprecated: true
      x-deprecated-reason: use getY instead
      x-deprecated-since: "1.2.0"
      x-sunset: "2026-08-01"
      parameters:
        - name: legacy
          in: query
          deprecated: true
          x-deprecated-reason: use filter instead
          x-sunset: "2027-01-15"
          schema: {type: string}
      responses:
        "200":
          description: ok
          headers:
            X-Legacy:
              deprecated: true
              x-deprecated-reason: header goes away
              schema: {type: string}
  /y:
    get:
      operationId: getY
      x-deprecated-reason: annotates nothing
      responses:
        "200":
          description: ok
components:
  schemas:
    Old:
      type: object
      deprecated: true
      x-deprecated-reason: replaced by New
      properties:
        p: {type: string, deprecated: true, x-deprecated-reason: field goes away}
        n: {type: string, deprecated: true, x-deprecated-reason: 7}
    Size:
      type: string
      enum: [small, large]
      x-extensible-enum: [small, large]
    Shade:
      type: string
      enum: [light, dark]
      x-extensible-enum: false
    Fixed:
      type: string
      enum: [one, two]
  securitySchemes:
    k:
      type: apiKey
      name: X-Key
      in: header
      deprecated: true
      x-deprecated-reason: rotate to oauth
