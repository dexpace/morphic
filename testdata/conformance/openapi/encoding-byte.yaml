openapi: 3.1.0
info: {title: EncodingByte, version: "1.0.0"}
paths: {}
components:
  schemas:
    Holder:
      type: object
      properties:
        # The bounds are declared beside the format on purpose: format: byte
        # hoists a node of its own, which is what stops the shared alias path
        # attaching the position's constraints afterwards.
        blob: {type: string, format: byte, minLength: 5, maxLength: 9}
