openapi: 3.1.0
info: {title: NamedTypes, version: "1.0.0"}
paths: {}
components:
  schemas:
    Address:
      type: object
      properties:
        city: {type: string}
    User:
      type: object
      properties:
        name: {type: string}
        address: {$ref: '#/components/schemas/Address'}
