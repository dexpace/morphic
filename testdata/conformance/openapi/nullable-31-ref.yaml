openapi: 3.1.0
info: {title: Nullable31Ref, version: "1.0.0"}
paths: {}
components:
  schemas:
    Target:
      type: [object, "null"]
    UnionTarget:
      oneOf: [{type: string}, {type: integer}, {type: "null"}]
    Collapsed:
      oneOf:
        - {type: string, description: the surviving branch}
        - {type: "null"}
    BranchRef: {$ref: '#/components/schemas/Collapsed/oneOf/0'}
    Owner:
      type: object
      properties:
        p: {$ref: '#/components/schemas/Target'}
        q: {$ref: '#/components/schemas/UnionTarget'}
