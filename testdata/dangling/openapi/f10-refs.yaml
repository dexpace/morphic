openapi: 3.1.0
info: {title: t, version: "1"}
paths: {}
components:
  schemas:
    Foo:
      properties:
        bar: {type: integer}
        x:
          $ref: '#/components/schemas/Foo/properties/bar'

