openapi: 3.1.0
info: {title: t, version: "1"}
paths: {}
components:
  schemas:
    M:
      properties:
        a: {type: string}
      additionalProperties:
        $ref: '#/components/schemas/M/properties/a'

