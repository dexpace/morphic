openapi: 3.1.0
info: {title: t, version: "1"}
paths: {}
components:
  schemas:
    Pet:
      oneOf:
        - {type: object, properties: {kind: {type: string}}}
      discriminator:
        propertyName: kind
        mapping:
          cat: '#/components/schemas/Pet/oneOf/0/properties/kind'
