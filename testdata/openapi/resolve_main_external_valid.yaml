openapi: 3.1.0
info: {title: Main, version: "1"}
paths:
  /a:
    get:
      operationId: a
      parameters:
        - {$ref: './resolve_target_valid.yaml#/components/parameters/Page'}
      responses:
        "200": {$ref: './resolve_target_valid.yaml#/components/responses/OK'}
